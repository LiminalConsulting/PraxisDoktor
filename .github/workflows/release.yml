name: Build & Release Windows EXE

on:
  push:
    tags:
      - "v*"

jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install uv
        run: pip install uv

      - name: Install dependencies
        run: uv sync

      - name: Install PyInstaller
        run: uv run pip install pyinstaller

      - name: Build PraxisDoktor.exe
        run: |
          uv run pyinstaller --onefile --name PraxisDoktor --noconsole `
            --add-data "static;static" `
            --add-data "vocab_default.json;." `
            main.py

      - name: Build PraxisDoktorSetup.exe (installer GUI)
        run: |
          uv run pyinstaller --onefile --name PraxisDoktorSetup --noconsole `
            installer_gui.py

      - name: Download Ollama installer
        run: |
          Invoke-WebRequest -Uri "https://ollama.com/download/OllamaSetup.exe" `
            -OutFile "dist\OllamaSetup.exe"
        shell: powershell

      - name: Copy ANLEITUNG.txt
        run: copy ANLEITUNG.txt dist\ANLEITUNG.txt

      - name: Create release ZIP
        run: |
          $tag = "${{ github.ref_name }}"
          # Copy app exe next to setup exe so setup can find it
          copy dist\PraxisDoktor.exe dist\PraxisDoktorSetup_folder\PraxisDoktor.exe 2>$null; `
          Compress-Archive `
            -Path dist\PraxisDoktorSetup.exe, dist\PraxisDoktor.exe, dist\OllamaSetup.exe, dist\ANLEITUNG.txt `
            -DestinationPath "PraxisDoktor-$tag.zip"
        shell: powershell

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: "PraxisDoktor-*.zip"
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
